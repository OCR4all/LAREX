file:
  upload-dir: ${java.io.tmpdir}/larex-test-uploads

larex:
  scheduling:
    enabled: false
  async:
    wait-for-tasks-to-complete-on-shutdown: false
  project-export:
    artifact-directory: ${java.io.tmpdir}/larex-test-export-artifacts
    minimum-free-bytes: 0
