# https://taskfile.dev

version: '3'

vars:
  VERSION:
    sh: cat VERSION
  DOCKER_REGISTRY: ghcr.io/ocr4all/larex
  COMPOSE_FILE: compose.yaml
  COMPOSE_DEV_DOCS_FILE: compose.dev.docs.yaml
  COMPOSE_PROD_BASE_FILE: compose.prod.base.yaml
  COMPOSE_PROD_AUTH_BUNDLED_FILE: compose.prod.auth.bundled-keycloak.yaml
  COMPOSE_PROD_AUTH_EXTERNAL_FILE: compose.prod.auth.external-keycloak.yaml
  COMPOSE_PROD_PUBLISH_FILE: compose.prod.publish.localhost.yaml
  COMPOSE_PROD_LOCAL_FILE: compose.prod.local.yaml
  COMPOSE_PROD_LOCAL_DOCS_FILE: compose.prod.local.docs.yaml
  COMPOSE_PROD_DOCS_FILE: compose.prod.docs.yaml
  COMPOSE_PROD_KRAKEN_FILE: compose.prod.kraken.yaml
  COMPOSE_PROD_NGINX_FILE: compose.prod.nginx.yaml

includes:
  backend:
    taskfile: ./backend/Taskfile.yml
    dir: ./backend
  docs:
    taskfile: ./docs/Taskfile.yml
    dir: ./docs
  frontend:
    taskfile: ./frontend/Taskfile.yml
    dir: ./frontend
  theme:
    taskfile: ./keycloak-theme/Taskfile.yml
    dir: ./keycloak-theme

tasks:
  default:
    desc: List all available tasks
    cmds:
      - task --list

  # ============================================================================
  # Development Setup
  # ============================================================================

  setup:
    desc: One-time setup for new developers
    cmds:
      - task: install
      - task: docker:infra
      - echo "Setup complete! Run 'task dev' to start development."

  install:
    desc: Install all dependencies
    cmds:
      - task: docs:install
      - task: frontend:install
      - task: theme:install
      - echo "All dependencies installed."

  dev:
    desc: Start development environment (requires infrastructure running)
    deps:
      - docker:infra
    cmds:
      - 'echo "Infrastructure is running. Start backend and frontend manually:"'
      - 'echo "  Terminal 1: task backend:dev"'
      - 'echo "  Terminal 2: task frontend:dev"'
      - 'echo ""'
      - 'echo "Or use Docker for everything: task docker:up"'

  # ============================================================================
  # Docker Orchestration
  # ============================================================================

  docker:up:
    desc: Start all services with Docker Compose
    cmds:
      - docker compose -f {{.COMPOSE_FILE}} up -d

  docker:up:build:
    desc: Build and start all services with Docker Compose
    cmds:
      - docker compose -f {{.COMPOSE_FILE}} up -d --build

  docker:up:docs:
    desc: Start local dev stack with self-hosted docs
    cmds:
      - docker compose -f {{.COMPOSE_FILE}} -f {{.COMPOSE_DEV_DOCS_FILE}} up -d

  docker:down:
    desc: Stop all services
    cmds:
      - docker compose -f {{.COMPOSE_FILE}} down

  docker:down:docs:
    desc: Stop local dev stack with docs override
    cmds:
      - docker compose -f {{.COMPOSE_FILE}} -f {{.COMPOSE_DEV_DOCS_FILE}} down

  docker:down:clean:
    desc: Stop all services and remove volumes
    cmds:
      - docker compose -f {{.COMPOSE_FILE}} down -v

  docker:infra:
    desc: Start only infrastructure services (postgres, keycloak, traefik)
    cmds:
      - docker compose -f {{.COMPOSE_FILE}} up -d postgres keycloak traefik

  docker:logs:
    desc: View logs from all services
    cmds:
      - docker compose -f {{.COMPOSE_FILE}} logs -f

  docker:logs:backend:
    desc: View backend logs
    cmds:
      - docker compose -f {{.COMPOSE_FILE}} logs -f app

  docker:logs:frontend:
    desc: View frontend logs
    cmds:
      - docker compose -f {{.COMPOSE_FILE}} logs -f frontend

  docker:logs:docs:
    desc: View docs logs in local dev stack
    cmds:
      - docker compose -f {{.COMPOSE_FILE}} -f {{.COMPOSE_DEV_DOCS_FILE}} logs -f docs

  docker:ps:
    desc: Show running containers
    cmds:
      - docker compose -f {{.COMPOSE_FILE}} ps

  # ============================================================================
  # Production Docker
  # ============================================================================

  docker:prod:up:
    desc: "Start opinionated production services (bundled Keycloak, external reverse proxy friendly)"
    cmds:
      - docker compose --env-file .env.prod -f {{.COMPOSE_PROD_BASE_FILE}} -f {{.COMPOSE_PROD_AUTH_BUNDLED_FILE}} -f {{.COMPOSE_PROD_PUBLISH_FILE}} up -d

  docker:prod:init-env:
    desc: Create .env.prod from template and seed required secrets
    cmds:
      - ./scripts/bootstrap-env-secrets.sh prod

  docker:prod:up:docs:
    desc: Start opinionated production services with self-hosted docs
    cmds:
      - docker compose --env-file .env.prod -f {{.COMPOSE_PROD_BASE_FILE}} -f {{.COMPOSE_PROD_AUTH_BUNDLED_FILE}} -f {{.COMPOSE_PROD_PUBLISH_FILE}} -f {{.COMPOSE_PROD_DOCS_FILE}} up -d

  docker:prod:up:kraken:
    desc: Start opinionated production services with the bundled Kraken segmentation processor
    cmds:
      - docker compose --env-file .env.prod -f {{.COMPOSE_PROD_BASE_FILE}} -f {{.COMPOSE_PROD_AUTH_BUNDLED_FILE}} -f {{.COMPOSE_PROD_PUBLISH_FILE}} -f {{.COMPOSE_PROD_KRAKEN_FILE}} up -d

  docker:prod:down:
    desc: Stop opinionated production services
    cmds:
      - docker compose --env-file .env.prod -f {{.COMPOSE_PROD_BASE_FILE}} -f {{.COMPOSE_PROD_AUTH_BUNDLED_FILE}} -f {{.COMPOSE_PROD_PUBLISH_FILE}} down

  docker:prod:down:docs:
    desc: Stop opinionated production services with docs override
    cmds:
      - docker compose --env-file .env.prod -f {{.COMPOSE_PROD_BASE_FILE}} -f {{.COMPOSE_PROD_AUTH_BUNDLED_FILE}} -f {{.COMPOSE_PROD_PUBLISH_FILE}} -f {{.COMPOSE_PROD_DOCS_FILE}} down

  docker:prod:logs:
    desc: View logs for opinionated production services
    cmds:
      - docker compose --env-file .env.prod -f {{.COMPOSE_PROD_BASE_FILE}} -f {{.COMPOSE_PROD_AUTH_BUNDLED_FILE}} -f {{.COMPOSE_PROD_PUBLISH_FILE}} logs -f

  docker:prod:logs:docs:
    desc: View logs for opinionated production services with docs
    cmds:
      - docker compose --env-file .env.prod -f {{.COMPOSE_PROD_BASE_FILE}} -f {{.COMPOSE_PROD_AUTH_BUNDLED_FILE}} -f {{.COMPOSE_PROD_PUBLISH_FILE}} -f {{.COMPOSE_PROD_DOCS_FILE}} logs -f docs

  docker:prod:down:kraken:
    desc: Stop opinionated production services with the bundled Kraken segmentation processor
    cmds:
      - docker compose --env-file .env.prod -f {{.COMPOSE_PROD_BASE_FILE}} -f {{.COMPOSE_PROD_AUTH_BUNDLED_FILE}} -f {{.COMPOSE_PROD_PUBLISH_FILE}} -f {{.COMPOSE_PROD_KRAKEN_FILE}} down

  docker:prod:logs:kraken:
    desc: View Kraken processor logs for the opinionated production stack
    cmds:
      - docker compose --env-file .env.prod -f {{.COMPOSE_PROD_BASE_FILE}} -f {{.COMPOSE_PROD_AUTH_BUNDLED_FILE}} -f {{.COMPOSE_PROD_PUBLISH_FILE}} -f {{.COMPOSE_PROD_KRAKEN_FILE}} logs -f kraken-segmentation-processor

  docker:prod:local:up:
    desc: Start production-like local stack (Traefik + local hostnames + Mailpit)
    cmds:
      - docker compose --env-file .env.prod.local -f {{.COMPOSE_PROD_BASE_FILE}} -f {{.COMPOSE_PROD_AUTH_BUNDLED_FILE}} -f {{.COMPOSE_PROD_LOCAL_FILE}} up -d

  docker:prod:local:init-env:
    desc: Create .env.prod.local from template and seed required secrets
    cmds:
      - ./scripts/bootstrap-env-secrets.sh prod-local

  docker:prod:local:up:docs:
    desc: Start production-like local stack with self-hosted docs
    cmds:
      - docker compose --env-file .env.prod.local -f {{.COMPOSE_PROD_BASE_FILE}} -f {{.COMPOSE_PROD_AUTH_BUNDLED_FILE}} -f {{.COMPOSE_PROD_LOCAL_FILE}} -f {{.COMPOSE_PROD_LOCAL_DOCS_FILE}} up -d

  docker:prod:local:up:kraken:
    desc: Start production-like local stack with the bundled Kraken segmentation processor
    cmds:
      - docker compose --env-file .env.prod.local -f {{.COMPOSE_PROD_BASE_FILE}} -f {{.COMPOSE_PROD_AUTH_BUNDLED_FILE}} -f {{.COMPOSE_PROD_LOCAL_FILE}} -f {{.COMPOSE_PROD_KRAKEN_FILE}} up -d

  docker:prod:local:down:
    desc: Stop production-like local stack
    cmds:
      - docker compose --env-file .env.prod.local -f {{.COMPOSE_PROD_BASE_FILE}} -f {{.COMPOSE_PROD_AUTH_BUNDLED_FILE}} -f {{.COMPOSE_PROD_LOCAL_FILE}} down

  docker:prod:local:down:docs:
    desc: Stop production-like local stack with docs override
    cmds:
      - docker compose --env-file .env.prod.local -f {{.COMPOSE_PROD_BASE_FILE}} -f {{.COMPOSE_PROD_AUTH_BUNDLED_FILE}} -f {{.COMPOSE_PROD_LOCAL_FILE}} -f {{.COMPOSE_PROD_LOCAL_DOCS_FILE}} down

  docker:prod:local:logs:
    desc: View logs for production-like local stack
    cmds:
      - docker compose --env-file .env.prod.local -f {{.COMPOSE_PROD_BASE_FILE}} -f {{.COMPOSE_PROD_AUTH_BUNDLED_FILE}} -f {{.COMPOSE_PROD_LOCAL_FILE}} logs -f

  docker:prod:local:logs:docs:
    desc: View logs for production-like local stack with docs
    cmds:
      - docker compose --env-file .env.prod.local -f {{.COMPOSE_PROD_BASE_FILE}} -f {{.COMPOSE_PROD_AUTH_BUNDLED_FILE}} -f {{.COMPOSE_PROD_LOCAL_FILE}} -f {{.COMPOSE_PROD_LOCAL_DOCS_FILE}} logs -f docs

  docker:prod:local:down:kraken:
    desc: Stop production-like local stack with the bundled Kraken segmentation processor
    cmds:
      - docker compose --env-file .env.prod.local -f {{.COMPOSE_PROD_BASE_FILE}} -f {{.COMPOSE_PROD_AUTH_BUNDLED_FILE}} -f {{.COMPOSE_PROD_LOCAL_FILE}} -f {{.COMPOSE_PROD_KRAKEN_FILE}} down

  docker:prod:local:logs:kraken:
    desc: View Kraken processor logs for the production-like local stack
    cmds:
      - docker compose --env-file .env.prod.local -f {{.COMPOSE_PROD_BASE_FILE}} -f {{.COMPOSE_PROD_AUTH_BUNDLED_FILE}} -f {{.COMPOSE_PROD_LOCAL_FILE}} -f {{.COMPOSE_PROD_KRAKEN_FILE}} logs -f kraken-segmentation-processor

  docker:prod:external-keycloak:up:
    desc: Start opinionated production services with external Keycloak
    cmds:
      - docker compose --env-file .env.prod -f {{.COMPOSE_PROD_BASE_FILE}} -f {{.COMPOSE_PROD_AUTH_EXTERNAL_FILE}} -f {{.COMPOSE_PROD_PUBLISH_FILE}} up -d

  docker:prod:external-keycloak:down:
    desc: Stop opinionated production services with external Keycloak
    cmds:
      - docker compose --env-file .env.prod -f {{.COMPOSE_PROD_BASE_FILE}} -f {{.COMPOSE_PROD_AUTH_EXTERNAL_FILE}} -f {{.COMPOSE_PROD_PUBLISH_FILE}} down

  docker:prod:external-keycloak:logs:
    desc: View logs for the external Keycloak production variant
    cmds:
      - docker compose --env-file .env.prod -f {{.COMPOSE_PROD_BASE_FILE}} -f {{.COMPOSE_PROD_AUTH_EXTERNAL_FILE}} -f {{.COMPOSE_PROD_PUBLISH_FILE}} logs -f

  docker:prod:nginx:up:
    desc: Start opinionated production services with bundled Nginx
    cmds:
      - docker compose --env-file .env.prod -f {{.COMPOSE_PROD_BASE_FILE}} -f {{.COMPOSE_PROD_AUTH_BUNDLED_FILE}} -f {{.COMPOSE_PROD_PUBLISH_FILE}} -f {{.COMPOSE_PROD_NGINX_FILE}} up -d

  docker:prod:nginx:down:
    desc: Stop opinionated production services with bundled Nginx
    cmds:
      - docker compose --env-file .env.prod -f {{.COMPOSE_PROD_BASE_FILE}} -f {{.COMPOSE_PROD_AUTH_BUNDLED_FILE}} -f {{.COMPOSE_PROD_PUBLISH_FILE}} -f {{.COMPOSE_PROD_NGINX_FILE}} down

  docker:prod:nginx:logs:
    desc: View logs for the bundled Nginx production variant
    cmds:
      - docker compose --env-file .env.prod -f {{.COMPOSE_PROD_BASE_FILE}} -f {{.COMPOSE_PROD_AUTH_BUNDLED_FILE}} -f {{.COMPOSE_PROD_PUBLISH_FILE}} -f {{.COMPOSE_PROD_NGINX_FILE}} logs -f

  # ============================================================================
  # Testing
  # ============================================================================

  test:
    desc: Run all checks (backend + frontend + docs)
    cmds:
      - task: test:backend
      - task: test:docs
      - task: test:frontend

  test:backend:
    desc: Run backend tests
    cmds:
      - task: backend:test

  test:docs:
    desc: Verify docs build
    cmds:
      - task: docs:test

  test:frontend:
    desc: Run frontend lint and typecheck
    cmds:
      - task: frontend:lint
      - task: frontend:typecheck

  # ============================================================================
  # Building
  # ============================================================================

  build:
    desc: Build all projects
    cmds:
      - task: backend:build
      - task: docs:build
      - task: frontend:build
      - task: theme:build

  build:docker:
    desc: Build production Docker images
    cmds:
      - 'docker build -f backend/Dockerfile.prod -t {{.DOCKER_REGISTRY}}/backend:{{.VERSION}} -t {{.DOCKER_REGISTRY}}/backend:latest ./backend'
      - 'docker build -f frontend/Dockerfile.prod -t {{.DOCKER_REGISTRY}}/frontend:{{.VERSION}} -t {{.DOCKER_REGISTRY}}/frontend:latest ./frontend'

  # ============================================================================
  # Versioning
  # ============================================================================

  ver:
    desc: Show current version
    cmds:
      - echo "Current version {{.VERSION}}"

  ver:sync:
    desc: Sync VERSION file to all projects
    cmds:
      - ./scripts/version-sync.sh

  ver:bump:major:
    desc: Bump major version (X.0.0)
    cmds:
      - |
        IFS='.' read -r major minor patch < VERSION
        echo "$((major + 1)).0.0" > VERSION
      - task: ver:sync
      - task: ver

  ver:bump:minor:
    desc: Bump minor version (0.X.0)
    cmds:
      - |
        IFS='.' read -r major minor patch < VERSION
        echo "${major}.$((minor + 1)).0" > VERSION
      - task: ver:sync
      - task: ver

  ver:bump:patch:
    desc: Bump patch version (0.0.X)
    cmds:
      - |
        IFS='.' read -r major minor patch < VERSION
        echo "${major}.${minor}.$((patch + 1))" > VERSION
      - task: ver:sync
      - task: ver

  # ============================================================================
  # Release
  # ============================================================================

  release:docker:
    desc: Build and tag Docker images for release
    cmds:
      - 'echo "Building release images for version {{.VERSION}}..."'
      - 'docker build -f backend/Dockerfile.prod -t {{.DOCKER_REGISTRY}}/backend:{{.VERSION}} -t {{.DOCKER_REGISTRY}}/backend:latest ./backend'
      - 'docker build -f frontend/Dockerfile.prod -t {{.DOCKER_REGISTRY}}/frontend:{{.VERSION}} -t {{.DOCKER_REGISTRY}}/frontend:latest ./frontend'

  release:docker:push:
    desc: Push Docker images to registry
    cmds:
      - 'docker push {{.DOCKER_REGISTRY}}/backend:{{.VERSION}}'
      - 'docker push {{.DOCKER_REGISTRY}}/backend:latest'
      - 'docker push {{.DOCKER_REGISTRY}}/frontend:{{.VERSION}}'
      - 'docker push {{.DOCKER_REGISTRY}}/frontend:latest'

  # ============================================================================
  # Toolkit
  # ============================================================================

  data:dump:
    desc: Create a full file-based data dump via admin backup API
    cmds:
      - ./scripts/larex-backup.sh dump --output "${LAREX_BACKUP_OUTPUT_PATH:-/mnt/data/backups}"

  data:reseed:
    desc: Reseed file-based data from a dump archive via admin backup API
    cmds:
      - ./scripts/larex-backup.sh reseed --source "${LAREX_RESEED_SOURCE_PATH:?set LAREX_RESEED_SOURCE_PATH}" --output "${LAREX_RESEED_OUTPUT_PATH:-/mnt/data/backups}"

  clean:
    desc: Clean all build artifacts
    cmds:
      - task: backend:clean
      - task: docs:clean
      - task: frontend:clean
      - task: theme:clean
      - echo "All build artifacts cleaned."

  status:
    desc: Show status of all services
    cmds:
      - 'echo "=== Docker Services ==="'
      - docker compose -f {{.COMPOSE_FILE}} ps
      - 'echo ""'
      - 'echo "=== Version ==="'
      - 'echo "Current version {{.VERSION}}"'

  db:shell:
    desc: Open PostgreSQL shell
    cmds:
      - 'docker compose -f {{.COMPOSE_FILE}} exec postgres psql -U larex -d larex_backend'

  keycloak:shell:
    desc: Open Keycloak container shell
    cmds:
      - 'docker compose -f {{.COMPOSE_FILE}} exec keycloak bash'
