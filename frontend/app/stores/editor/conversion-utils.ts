/**
 * Legacy conversion utils.
 *
 * The editor now uses the PAGE 2019 `PcGts` model as the single source of truth.
 * Deprecated helper-model conversion functions were removed.
 */

export { pointToVertex, vertexToPoint, pointsToVertices, verticesToPoints } from '@/commands/editor/utils'
