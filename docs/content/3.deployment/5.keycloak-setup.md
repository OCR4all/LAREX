---
title: Keycloak Setup for Institutions
description: Configure Keycloak for institutional authentication with LDAP user federation.
navigation:
  icon: i-lucide-users
seo:
  title: Keycloak Institutional Setup
  description: Guide to configuring Keycloak with LDAP for institutional authentication.
---

# Keycloak Setup for Institutions

This guide covers configuring Keycloak for institutional usage, including LDAP user federation and SSO integration.

## Overview

For production deployments at institutions, you typically need to:

- **Federate existing user directories** (LDAP/Active Directory)
- **Enable Single Sign-On (SSO)** with institutional identity providers
- **Configure user attribute mapping** from the directory to LAREX
- **Set up authorization policies** based on directory groups

## LAREX Bootstrap Realm Template

LAREX ships `config/keycloak/larex.bootstrap.realm.json` as a bootstrap template for fresh realms.

### What the template configures

- Realm role: `GLOBAL_ADMIN`
- Backend service-account client: `larex-backend-service`
- Service-account permissions: only `query-users`, `view-users`, `manage-users` (no `realm-admin`)
- Frontend client: `larex-frontend`
- Initial seeded admin user from environment variables (`LAREX_INITIAL_ADMIN_*`)

### Bundled Keycloak deployments

`compose.prod.yaml` and `compose.prod.traefik.yaml` run Keycloak with `--import-realm` and mount the bootstrap template automatically.

### External Keycloak deployments

For `compose.prod.external-keycloak.yaml`, import the template manually or recreate the same configuration in your existing realm.

### Required bootstrap variables

- `KEYCLOAK_ADMIN_REALM`
- `KEYCLOAK_ADMIN_CLIENT_SECRET`
- `NUXT_OAUTH_KEYCLOAK_CLIENT_SECRET`
- `NUXT_OAUTH_KEYCLOAK_REDIRECT_URL`
- `CORS_ALLOWED_ORIGIN`
- `LAREX_INITIAL_ADMIN_USERNAME`
- `LAREX_INITIAL_ADMIN_EMAIL`
- `LAREX_INITIAL_ADMIN_FIRST_NAME`
- `LAREX_INITIAL_ADMIN_LAST_NAME`
- `LAREX_INITIAL_ADMIN_TEMP_PASSWORD`

### Post-bootstrap hardening

- Log in once as the seeded initial admin.
- Change the initial admin temporary password immediately.
- Rotate/remove bootstrap credentials (`KEYCLOAK_ADMIN_PASSWORD`, `LAREX_INITIAL_ADMIN_TEMP_PASSWORD`) after successful setup.

## User Federation with LDAP

### Enabling LDAP Federation

1. Access Keycloak Admin Console: `https://auth.your-domain.com/admin`
2. Navigate to **User Federation** → **Add provider** → **ldap**
3. Configure the LDAP connection:

```yaml
# Example LDAP Configuration
Connection URL: ldap://ldap.institution.edu:389
Users DN: ou=users,dc=institution,dc=edu
Bind DN: cn=admin,dc=institution,dc=edu
Bind Credential: your-service-account-password
```

4. Set synchronization options:
   - **Edit Mode**: `READ_ONLY` (recommended) or `WRITABLE`
   - **Sync Registrations**: Enable to import new users
   - **User LDAP Mappings**: Configure attribute mappings

### Supported LDAP Modes

| Mode | Description |
|------|-------------|
| `READ_ONLY` | Users cannot be modified in Keycloak; changes must be made in LDAP |
| `WRITABLE` | Users can be modified in Keycloak and synced back to LDAP |
| `UNSYNCED` | Users exist in both systems independently |

### LAREX behavior with LDAP federation

LAREX does not try to inspect Keycloak federation provider configuration with elevated service-account roles. Instead, set `LAREX_USER_PROVISIONING_MODE=LDAP_MANAGED` explicitly in deployments where LDAP or AD is authoritative for user lifecycle.

In `LDAP_MANAGED` mode:

- LAREX admin user management becomes read-only for lifecycle actions
- creating users in LAREX is disabled
- Keycloak setup-email/password onboarding from LAREX is disabled
- LDAP-backed users detected through `federationLink` are shown as externally managed

This is intentional. It preserves the least-privilege backend Keycloak client and keeps account creation, password resets, and lifecycle management in your directory or institutional IdP.

### Active Directory Configuration

For Active Directory:

```yaml
Connection URL: ldap://ad.institution.edu:389
Users DN: CN=Users,DC=institution,DC=edu
Kerberos Realm: INSTITUTION.EDU
```

### LDAP Attribute Mapping

Map LDAP attributes to Keycloak user attributes:

| LDAP Attribute | Keycloak Attribute |
|----------------|-------------------|
| `uid` | username |
| `mail` | email |
| `givenName` | firstName |
| `sn` | lastName |
| `displayName` | displayName |
| `memberOf` | groups |

See [Keycloak LDAP Documentation](https://www.keycloak.org/docs/latest/server_admin/#_ldap) for details.

## Identity Provider Federation

### SAML 2.0 Integration

For SAML-based SSO:

1. Create a new Identity Provider: **Identity Providers** → **Add provider** → **saml**
2. Configure SAML settings:

```yaml
Single Sign-On Service URL: https://idp.institution.edu/sso
Single Logout Service URL: https://idp.institution.edu/slo
Entity ID: https://auth.your-domain.com/realms/larex
Want AuthnRequests Signed: true
```

3. Export SP metadata and provide to your IdP administrator

### OIDC Integration

For OIDC-based SSO:

1. Create a new Identity Provider: **Identity Providers** → **Add provider** → **oidc**
2. Configure OIDC settings:

```yaml
Authorization URL: https://idp.institution.edu/authorize
Token URL: https://idp.institution.edu/token
User Info URL: https://idp.institution.edu/userinfo
Client ID: larex-app
Client Secret: your-client-secret
```

See [Keycloak Identity Provider Documentation](https://www.keycloak.org/docs/latest/server_admin/#_identity_broker) for details.

## Group and Role Management

### LDAP Group Mapping

1. Configure LDAP group mapper:
   - **Mapper Type**: `group-ldap-mapper`
   - **LDAP Groups DN**: `ou=groups,dc=institution,dc=edu`
   - **Group Name LDAP Attribute**: `cn`

2. Map LDAP groups to Keycloak roles:

```yaml
# Example group-to-role mapping
larex-admins -> larex-admin
larex-editors -> larex-editor
larex-viewers -> larex-user
```

### Role-Based Access Control

Configure realm roles in Keycloak:

1. Navigate to **Realm Roles** → **Create**
2. Define roles: `admin`, `editor`, `viewer`
3. Assign roles to users or groups

Configure client roles for LAREX:

```yaml
Client ID: larex-frontend
Roles:
  - user
  - editor
  - admin
```

See [Keycloak Role Documentation](https://www.keycloak.org/docs/latest/server_admin/#_roles) for details.

## User Session Management

### Session Configuration

1. Navigate to **Realm Settings** → **Sessions**
2. Configure session settings:

```yaml
SSO Session Idle Timeout: 8 hours
SSO Session Max Lifespan: 30 days
User Session Max Lifespan: 8 hours
Login Session Timeout: 60 minutes
```

### Token Configuration

Navigate to **Realm Settings** → **Tokens**:

```yaml
Access Token Lifespan: 5 minutes
Access Token Lifespan for Implicit Flow: 15 minutes
Refresh Token Lifespan: 30 days
ID Token Lifespan: 5 minutes
```

## Multi-Realm Setup

For large institutions, consider separate realms:

| Realm | Purpose |
|-------|---------|
| `larex-prod` | Production LAREX |
| `larex-staging` | Staging environment |
| `larex-dev` | Development testing |

## Email Configuration

For password resets and notifications:

```yaml
# In compose.prod.yaml
environment:
  MAIL_HOST: smtp.institution.edu
  MAIL_PORT: 587
  MAIL_FROM: noreply@larex.institution.edu
```

Configure in Keycloak: **Realm Settings** → **Email**

## Security Considerations

### Password Policies

Navigate to **Realm Settings** → **Password Policy**:

```yaml
Type: Password History
History Size: 10
Minimum Length: 12
Maximum Length: 100
Not Username: true
```

### Brute Force Protection

Navigate to **Realm Settings** → **Security Defenses**:

```yaml
Brute Force Detection:
  Enabled: true
  Max Login Failures: 5
  Wait Increment: 1 minute
  Quick Login Check Milliseconds: 500
```

### SSL/TLS Configuration

Ensure HTTPS is enforced:

```yaml
environment:
  KC_HOSTNAME_STRICT: true
  KC_HOSTNAME_STRICT_HTTPS: true
  KC_PROXY: edge
```

## Troubleshooting

### LDAP Connection Issues

```bash
# Test LDAP connection
ldapsearch -H ldap://ldap.institution.edu -D "cn=admin,dc=institution,dc=edu" -W -b "dc=institution,dc=edu" "(objectclass=*)"
```

### Sync Issues

Check synchronization logs in Keycloak Admin Console:
**User Federation** → **LDAP** → **Synchronize all users**

### User Import Issues

1. Verify LDAP attribute mapping
2. Check for duplicate users
3. Ensure required attributes are present

## External Resources

- [Keycloak Server Administration Guide](https://www.keycloak.org/docs/latest/server_admin/)
- [LDAP Federation](https://www.keycloak.org/docs/latest/server_admin/#_ldap)
- [Identity Brokering](https://www.keycloak.org/docs/latest/server_admin/#_identity_broker)
- [SAML Configuration](https://www.keycloak.org/docs/latest/server_admin/#_saml)
- [OIDC Configuration](https://www.keycloak.org/docs/latest/server_admin/#_oidc)

## Next Steps

- [Environment Variables](/deployment/environment-vars) - Configure email and auth settings
- [Service Reference](/deployment/service-reference) - Keycloak service configuration
