---
title: Command Reference
description: Taskfile and Docker commands for the supported LAREX setups.
navigation:
  icon: i-lucide-terminal
seo:
  title: Command Reference
  description: Common LAREX development and deployment commands.
---

# Command Reference

All root commands are run as `task <name>` unless noted otherwise.

The Docker tasks are the supported way to run LAREX locally and in production. Native install/build tasks are for maintainers who run individual services outside Docker or who need local checks before committing.

## Common Task Commands

| Command | Description |
|---------|-------------|
| `task setup` | Optional native-helper setup: installs JavaScript dependencies and starts Docker infrastructure |
| `task install` | Optional native-helper install for docs, frontend, and Keycloak theme dependencies |
| `task dev` | Prints the hybrid native startup flow and points to `task docker:up` for all-Docker development |
| `task test` | Run backend, frontend, and docs checks on the host |
| `task build` | Build backend, docs, frontend, and theme on the host |
| `task clean` | Remove build artifacts |

## Docker Tasks

### Local dev

| Command | Description |
|---------|-------------|
| `task docker:up` | Start the supported local Docker development stack |
| `task docker:up:build` | Rebuild and start the local dev stack |
| `task docker:up:docs` | Start local dev stack with self-hosted docs |
| `task docker:actions:init-env` | Create `.env.actions` and generate official processor secrets |
| `task docker:actions:up` | Start local dev with all official Action processors |
| `task docker:actions:down` | Stop local dev with official Action processors |
| `task docker:actions:logs` | Follow local dev and processor logs |
| `task docker:actions:ps` | Show local dev and processor status |
| `task docker:down` | Stop the local dev stack |
| `task docker:down:docs` | Stop local dev stack with docs override |
| `task docker:down:clean` | Stop the local dev stack and remove volumes |
| `task docker:infra` | Start only shared infrastructure for optional native service development |
| `task docker:logs` | View local dev logs |
| `task docker:logs:backend` | View backend logs |
| `task docker:logs:frontend` | View frontend logs |
| `task docker:logs:docs` | View docs logs in local dev stack |
| `task docker:ps` | Show local dev containers |

### Production-like and production

| Command | Description |
|---------|-------------|
| `task docker:prod:init-env` | Create `.env.prod` from the template and seed required secrets |
| `task docker:prod:up` | Start the opinionated production stack |
| `task docker:prod:up:docs` | Start opinionated production with self-hosted docs |
| `task docker:prod:config` | Validate the resolved production Compose model |
| `task docker:prod:ps` | Show production container and health status |
| `task docker:prod:pull` | Pull the currently pinned production images |
| `task docker:prod:upgrade` | Pull and apply an upgrade, then wait for health |
| `task docker:prod:down` | Stop the opinionated production stack |
| `task docker:prod:down:docs` | Stop opinionated production with docs override |
| `task docker:prod:logs` | View opinionated production logs |
| `task docker:prod:logs:docs` | View docs logs in opinionated production |
| `task docker:prod:actions:up` | Start opinionated production with official Action processors |
| `task docker:prod:actions:down` | Stop opinionated production with official Action processors |
| `task docker:prod:actions:logs` | Follow opinionated production and processor logs |
| `task docker:prod:actions:ps` | Show production and processor health status |
| `task docker:prod:actions:pull` | Pull pinned production and processor images |
| `task docker:prod:actions:upgrade` | Pull and apply an upgrade including processors |
| `task docker:prod:local:init-env` | Create `.env.prod.local` from the template and seed required secrets |
| `task docker:prod:local:up` | Start local production-like services |
| `task docker:prod:local:up:docs` | Start local production-like services with docs |
| `task docker:prod:local:config` | Validate the local production-like Compose model |
| `task docker:prod:local:ps` | Show local production-like container status |
| `task docker:prod:local:down` | Stop local production-like services |
| `task docker:prod:local:down:docs` | Stop local production-like services with docs |
| `task docker:prod:local:logs` | View local production-like logs |
| `task docker:prod:local:logs:docs` | View docs logs in local production-like setup |
| `task docker:prod:local:actions:up` | Start local production-like stack with official Action processors |
| `task docker:prod:local:actions:down` | Stop local production-like stack with official Action processors |
| `task docker:prod:local:actions:logs` | Follow local production-like and processor logs |
| `task docker:prod:external-keycloak:up` | Start production with external Keycloak |
| `task docker:prod:external-keycloak:down` | Stop production with external Keycloak |
| `task docker:prod:external-keycloak:logs` | View production logs for external Keycloak |
| `task docker:prod:nginx:up` | Start production with bundled Nginx |
| `task docker:prod:nginx:down` | Stop production with bundled Nginx |
| `task docker:prod:nginx:logs` | View production logs for bundled Nginx |

## Direct Docker Compose Commands

Use direct Docker Compose commands when Taskfile is not installed. They are equivalent to the Taskfile shortcuts.

### Local dev

```bash
docker compose up -d
docker compose logs -f
docker compose down
```

With self-hosted docs:

```bash
docker compose -f compose.yaml -f compose.dev.docs.yaml up -d
```

With every official Action processor:

```bash
scripts/bootstrap-env-secrets.sh actions
docker compose --env-file .env.actions \
  -f compose.yaml \
  -f compose.actions.yaml \
  --profile actions \
  up -d --build
```

### Local production-like

```bash
docker compose --env-file .env.prod.local \
  -f compose.prod.base.yaml \
  -f compose.prod.auth.bundled-keycloak.yaml \
  -f compose.prod.local.yaml \
  up -d --wait --wait-timeout 300
```

With self-hosted docs:

```bash
docker compose --env-file .env.prod.local \
  -f compose.prod.base.yaml \
  -f compose.prod.auth.bundled-keycloak.yaml \
  -f compose.prod.local.yaml \
  -f compose.prod.local.docs.yaml \
  up -d --wait --wait-timeout 300
```

With every official Action processor, add the independent Action env and fragment:

```bash
docker compose \
  --env-file .env.prod.local \
  --env-file .env.actions \
  -f compose.prod.base.yaml \
  -f compose.prod.auth.bundled-keycloak.yaml \
  -f compose.prod.local.yaml \
  -f compose.actions.yaml \
  --profile actions \
  up -d --wait --wait-timeout 300
```

### Opinionated production

```bash
docker compose --env-file .env.prod \
  -f compose.prod.base.yaml \
  -f compose.prod.auth.bundled-keycloak.yaml \
  -f compose.prod.publish.localhost.yaml \
  up -d --wait --wait-timeout 300
```

With self-hosted docs:

```bash
docker compose --env-file .env.prod \
  -f compose.prod.base.yaml \
  -f compose.prod.auth.bundled-keycloak.yaml \
  -f compose.prod.publish.localhost.yaml \
  -f compose.prod.docs.yaml \
  up -d --wait --wait-timeout 300
```

With every official Action processor:

```bash
docker compose \
  --env-file .env.prod \
  --env-file .env.actions \
  -f compose.prod.base.yaml \
  -f compose.prod.auth.bundled-keycloak.yaml \
  -f compose.prod.publish.localhost.yaml \
  -f compose.actions.yaml \
  --profile actions \
  up -d --wait --wait-timeout 300
```

### Production with external Keycloak

```bash
docker compose --env-file .env.prod \
  -f compose.prod.base.yaml \
  -f compose.prod.auth.external-keycloak.yaml \
  -f compose.prod.publish.localhost.yaml \
  up -d --wait --wait-timeout 300
```
